# This example is a standalone project, not a member of the repo's pnpm
# workspace (pnpm-workspace.yaml only includes packages/*). Without this,
# `pnpm install` run here would resolve against the workspace root instead
# of installing locally.
ignore-workspace=true
