{
  "name": "@felix/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Workers version of the Felix orchestrator (manifest-driven agent runtime with strict A2A/MCP/OpenAI parity)",
  "type": "module",
  "exports": {
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build:manifests": "tsx scripts/bundle-manifests.ts",
    "build:docs": "tsx scripts/bundle-docs.ts",
    "build": "pnpm build:manifests && pnpm build:docs",
    "dev": "pnpm build:manifests && pnpm build:docs && wrangler dev",
    "dev:remote": "pnpm build:manifests && pnpm build:docs && wrangler dev --remote",
    "deploy": "pnpm build:manifests && pnpm build:docs && wrangler deploy --env production",
    "deploy:staging": "pnpm build:manifests && pnpm build:docs && wrangler deploy --env staging",
    "migrate:local": "wrangler d1 migrations apply orchestrator --local",
    "migrate:staging": "wrangler d1 migrations apply orchestrator-staging --remote --env staging",
    "migrate:production": "wrangler d1 migrations apply orchestrator-prod --remote --env production",
    "setup:vectorize:staging": "bash scripts/setup-vectorize.sh felix-memory-staging",
    "setup:vectorize:production": "bash scripts/setup-vectorize.sh felix-memory-prod",
    "eval": "tsx scripts/eval.ts"
  },
  "dependencies": {
    "@felix/commerce": "workspace:*",
    "@hono/zod-openapi": "^1.4.0",
    "@scalar/hono-api-reference": "^0.10.14",
    "hono": "^4.12.18",
    "jose": "^6.2.3",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260511.1",
    "marked": "^16.4.2",
    "tsx": "^4.21.0",
    "wrangler": "^4.90.1",
    "yaml": "^2.9.0"
  },
  "engines": {
    "node": ">=20"
  }
}
